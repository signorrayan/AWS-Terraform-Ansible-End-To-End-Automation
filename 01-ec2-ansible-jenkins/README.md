# EC2-Ansible-Jenkins

Using this configuration, you can create an end-to-end automation to install jenkins on ec2 using ansible. (All will be done by Terraform)


```
terraform init
terraform validate
terraform plan
terrafom apply
```